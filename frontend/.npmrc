# @vitejs/plugin-react 4.7 predates Vite 8 and does not list it as a peer, although it works with it.
# Without this, `npm install` stops with ERESOLVE. Remove once the plugin is upgraded.
legacy-peer-deps=true
